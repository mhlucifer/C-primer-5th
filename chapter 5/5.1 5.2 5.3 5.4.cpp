//5.1 空语句就是指只有一个分号，比如循环的全部工作再条件中就可以完成，此时使用空语句
//5.2块是指用花括号括起来的地方。
//5.3 while (val <= 10)
//sum += val, ++val; 感觉不如之前的
//5.4 这个循环试图遍历string，但是这里在循环内部定义了变量，这会导致每次循环
//被重新初始化一次
// 这里两个变量在自己的域中定义，所以它们无法访问到对方

